interface:
  display_name: "技能创建器"
  short_description: "生成官方兼容、Kun 可治理的新 Skill 骨架"
  default_prompt: "使用 $skill-creator 创建一个官方兼容的新 Agent Skill 骨架，并先输出兼容判断。"
policy:
  allow_implicit_invocation: true
  invocation_policy: on_demand
