interface:
  display_name: "技能修复器"
  short_description: "修复并精炼已有技能"
  default_prompt: "使用 $skill-modify 对单个技能进行审计与重构，优先做最小但高影响的提示词修复。"
policy:
  allow_implicit_invocation: true
  invocation_policy: on_demand
